<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianji.promotion.mapper.CouponMapper">

    <update id="incrUsedNum">
        update coupon set used_num = used_num + #{i}
        where id in
        <foreach collection="couponIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        # 让"减"只允许在 used_num > 0 时执行
        and ((#{i} > 0 and used_num &lt; total_num) or (#{i} &lt; 0 and used_num > 0))
    </update>
</mapper>
